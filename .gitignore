# Security
.env
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.venv/
venv/
env/

# Build/Distribution
dist/
build/
*.spec
publish/

# C# / .NET
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates

# Artifacts & Builds (Keep small logs/reports, exclude binaries)
artifacts/builds/
artifacts/batch/outputs/
artifacts/*.exe
artifacts/*.dll
artifacts/*.pdb

# Handoff / Artifacts
artifacts/handoff/from_codex/
artifacts/handoff/to_codex/
!artifacts/handoff/registry.json

# Large Files (>50MB - GitHub hard limit is 100MB)
# We use 50MB to avoid warnings
# Exception: Source archives for deps are OK if compressed and <50MB
*.exe
*.dll
*.pdb
*.so
*.dylib

# Large Archives (>50MB)
# Small archives (<50MB) are OK if they're source code or data
*-large.zip
*-large.tar.gz
*-large.rar
*.iso
*.bin

# OS
.DS_Store
Thumbs.db

# Logs (keep small ones, exclude large dumps)
*.log.*.gz
*.log.[0-9]*
*-large.log
